// Retired: the spatial floor plan was replaced by area/building targeting and the
// Areas page's screen list. Kept as an empty stub because the sandbox can't delete
// files — safe to remove this file in a cleanup pass.
export default function SignageFloorPlanBuilderRetired() {
  return null
}
