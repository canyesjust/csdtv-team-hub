// Retired: the image-based floor map is no longer used. Kept as an empty stub
// because the sandbox can't delete files — safe to remove in a cleanup pass.
export default function SignageFloorMapRetired() {
  return null
}
